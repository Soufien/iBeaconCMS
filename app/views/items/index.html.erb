<h1>Listing items</h1>

<table>
  <thead>
    <tr>
      <th>Spec</th>
      <th>Name</th>
      <th>Description</th>
      <th>Beacon</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @items.each do |item| %>
      <tr>
        <td><%= item.spec %></td>
        <td><%= item.name %></td>
        <td><%= item.description %></td>
        <td><%= item.beacon.major.to_s + ' ' + item.beacon.minor.to_s %></td>
        <td><%= link_to 'Show', item %></td>
        <td><%= link_to 'Edit', edit_item_path(item) %></td>
        <td><%= link_to 'Destroy', item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Item', new_item_path %>
